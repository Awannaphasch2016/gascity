# bridge.py talks to Telegram over plain HTTP and receives Gas City's outbound
# callbacks on a Flask listener. python-telegram-bot is deliberately absent: the
# bridge sends from both a request handler and a polling thread, and an asyncio
# client conflicts with both.
flask==3.1.3
requests==2.33.1
